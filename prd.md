# Nuclei GUI — 产品需求文档（PRD）

> 版本：v1.0　日期：2026-08-31　状态：已确认（含第一版已实现功能 + 待开发项）
> 本文档是后续开发的唯一需求依据。需求变更必须更新本文档。

---

## 1. 整体代码、页面和导航结构

### 1.1 技术架构
- **形态**：本地 Web 应用。后端用**纯 Python 标准库**（`http.server` + 线程），**零第三方依赖**；前端为 Win11 Fluent 风格的单页 Web 界面。
- **跨平台**：目标平台 Kali Linux（Debian 系），同时兼容 Windows / macOS。
- **免登录**：默认监听 `127.0.0.1`，无账号体系，浏览器直接访问。
- **命令调度**：后端根据界面参数构造真实 `nuclei` CLI 参数，用子进程执行；前端轮询获取实时日志与结果。

### 1.2 代码与目录结构
```
nuclei-gui/
├── app.py              # 后端：HTTP 服务 + nuclei 调度 + 数据持久化 + 镜像下载 + 模板管理
├── start.sh            # Kali 一键启动脚本（sudo）
├── start.bat           # Windows 启动脚本
├── README.md           # 使用说明
├── prd.md              # 本文档（需求依据）
├── DEVELOPMENT_PLAN.md # 开发计划
├── web/                # 前端
│   ├── index.html      # 页面结构（4 个页面 + 弹窗）
│   ├── css/style.css   # Win11 风格样式（深浅主题）
│   └── js/app.js       # 前端逻辑（路由/API/扫描/历史/模板/设置）
├── custom-templates/   # 自定义可复用 YAML 模板（nuclei 同级目录）
└── data/               # 持久化数据（详见第 7 节）
```

### 1.3 页面与导航结构
| 导航项 | 页面 | 主要职责 |
|---|---|---|
| 扫描 | `#page-scan` | 配置并启动/停止扫描，实时日志 |
| 历史记录 | `#page-history` | 扫描记录列表、详情（结果/日志/命令）、导出 |
| 模板库 | `#page-templates` | 浏览已安装模板/标签、我的模板、YAML 编辑器 |
| 设置 | `#page-settings` | 引擎路径、默认参数、网络与更新、备份恢复 |

全局元素：顶部标题栏（应用名、引擎状态、主题切换）、左侧导航（含引擎/模板版本角标）、Toast 提示。

---

## 2. 页面内容

### 2.1 扫描页
- **目标**：单目标（`-u`）/ 多目标（textarea，`-l` 文件）切换。
- **模板与过滤**：严重程度多选（info/low/medium/high/critical）、标签 tags、排除标签 etags、指定模板/目录 -t、协议类型多选 -pt。
- **速率与优化**：速率限制 -rl、并发 -c、批量 -bs、超时 -timeout、重试 -retries、代理 -proxy；开关：跟随重定向、Headless、仅输出结果(silent)、详细输出(-v)、保存请求/响应。
- **高级参数**：自定义请求头 -H、附加原始参数。
- **操作区**：开始扫描 / 停止；实时输出控制台 + 结果计数徽章。

### 2.2 历史记录页
- 扫描记录表格：状态、目标、过滤条件、结果数、时间、操作（详情/删除）。
- **详情弹窗**：三个标签页——结果（严重度筛选、YAML 生成、JSON 查看、导出）、日志（原始输出）、命令（本次执行的 nuclei 命令）。

### 2.3 模板库页
- 筛选区（严重程度/标签/类型）→ 加载模板列表（`nuclei -tl`）。
- 模板列表：点击「填入扫描」。
- 标签一览（`nuclei -tgl`）：点击加入扫描标签。
- **我的模板**：自定义 YAML 模板列表，操作：查看/编辑/使用/删除；「新建模板」。
- **YAML 编辑器弹窗**：文件名、内容编辑区、保存 / 保存并用于扫描 / 取消；来源（内置模板 / 自动骨架）。

### 2.4 设置页
- 引擎：nuclei 路径、自动检测、引擎状态、数据目录、模板目录、自定义模板目录。
- 网络与更新：网络连通性检测、更新模板库（官方→镜像）、强制镜像下载模板、更新 nuclei、镜像下载 nuclei、更新输出。
- 默认扫描参数：速率/并发/批量/超时/重试、允许自动检查更新。

---

## 3. 功能模块与结构

| 模块 | 后端 | 前端 |
|---|---|---|
| 引擎探测 | `resolve_nuclei` / `get_nuclei_status` / `run_cli` | 状态徽章、设置引擎框 |
| 扫描调度 | `ScanManager` / `ScanRunner`（线程+子进程） | 扫描表单、实时轮询 |
| 历史持久化 | `load_scans` / `save_scans` / `scan_record` / `update_scan` | 历史表格、详情弹窗 |
| 结果解析 | `parse_jsonl_results` | 结果表格、严重度筛选 |
| 模板浏览 | `/api/templates`（`-tl`）/ `/api/tags`（`-tgl`） | 模板/标签列表 |
| 自定义模板 | `save/read/list/delete_custom_template` / `generate_yaml_from_result` / `find_builtin_template` | 我的模板、YAML 编辑器 |
| 镜像联网 | `http_get_bounded` / `download_with_mirror` / `update_templates_from_mirror` / `download_nuclei_from_mirror` / `/api/netcheck` | 网络检测、更新按钮 |
| 配置 | `get_config` / `save_config` | 设置页 |

---

## 4. 每个模块的用途

1. **引擎探测**：定位 nuclei 二进制（配置路径→应用同目录→PATH），显示版本与模板版本，作为扫描前置条件。
2. **扫描调度**：把界面参数映射为 nuclei CLI 参数并执行；实时收集 stdout 写入日志与缓冲；支持停止；结束时统计结果数。
3. **历史持久化**：把每次扫描的元信息（目标、时间、状态、结果数、命令、过滤条件）写入 `data/scans.json`，重启不丢。
4. **结果解析**：把 nuclei `-jle` 输出的 JSONL 解析为前端表格行（模板、名称、严重度、目标、匹配器等）。
5. **模板浏览**：读取本机已安装模板与标签，辅助用户选择模板/标签。
6. **自定义模板**：把历史漏洞一键生成可复用 YAML 模板，可查看/保存/修改/删除/复用。
7. **镜像联网**：解决国内直连 GitHub 失败问题——官方源更新失败自动回退镜像（Gitee zip/git、GitHub 加速代理）下载模板与 nuclei 二进制；检测各源连通性。
8. **配置**：持久化引擎路径、默认参数、主题等。

---

## 5. 每个模块的必要数据和操作

| 模块 | 必要数据 | 必要操作 |
|---|---|---|
| 引擎探测 | nuclei 路径、版本、模板版本 | 检测、路径自动查找 |
| 扫描调度 | targets、severity、tags、etags、types、templates、rl/c/bs/timeout/retries、proxy、headers、开关、extra_args | 启动、停止、读取实时日志、写结果文件 |
| 历史持久化 | 扫描元信息（JSON 列表） | 增（插入新扫描）、改（状态/结果数/时间）、删、查 |
| 结果解析 | JSONL 文件 | 读文件→结构化→按严重度筛选 |
| 模板浏览 | `-tl`/`-tgl` 命令输出 | 加载、本地搜索、填入扫描 |
| 自定义模板 | name、content（YAML 文本）、文件 | 生成、保存、读取、列表、删除；文件名防穿越 |
| 镜像联网 | 镜像地址列表、下载数据 | 探测连通、官方更新、镜像下载 zip/clone、解压到模板目录 |
| 配置 | 引擎路径、默认参数、主题 | 读、写 |

---

## 6. 首页与每个模块的关系

- 打开应用即进入**扫描页（首页）**，是唯一入口和主操作台。
- 扫描页 → 启动扫描 → 历史页出现记录 → 详情中可查看结果、生成 YAML。
- 扫描页 → 模板库选择模板/标签 → 填入扫描表单。
- 历史 → YAML → 我的模板 → 再回到扫描页复用。
- 设置页为全局基础配置，影响扫描默认值、引擎路径、网络更新、备份恢复。
- 首页状态徽章实时反映引擎是否可用，引导用户进入设置页安装/配置。

---

## 7. 本地数据和文件、备份和恢复需求

### 7.1 数据文件清单
| 文件/目录 | 内容 | 可否重建 |
|---|---|---|
| `data/config.json` | 配置（引擎路径、默认参数、主题） | 可重建（默认值） |
| `data/scans.json` | 扫描历史元信息 | 不可重建 |
| `data/results/<id>.jsonl` | 结构化漏洞结果 | 不可重建（可重新扫描） |
| `data/results/<id>.txt` | 文本报告 | 可重建 |
| `data/logs/<id>.log` | 原始日志 | 不可重建 |
| `data/targets/<id>.txt` | 多目标列表 | 不可重建（可重新输入） |
| `custom-templates/*.yaml` | 自定义复用模板 | **重要，不可重建** |

### 7.2 持久化要求
- 重启 / 关闭 / 刷新均不丢失数据。
- 应用目录放在 nuclei 同级目录，即数据保存在 nuclei 同级目录。

### 7.3 备份与恢复需求（待开发）
- **备份**：一键把 `data/`（配置、历史、结果、日志）+ `custom-templates/` 打包为 zip，可指定文件名，下载到本地。
- **恢复**：选择 zip 文件导入，覆盖恢复对应目录（先备份当前数据再覆盖，避免误操作丢失）。
- 备份文件命名建议含时间戳；恢复前必须校验 zip 结构合法。

---

## 8. 第一版必要的功能（已完成）

1. Win11 风格界面（亚克力、圆角、深浅主题，主题蓝）。
2. 扫描配置映射 nuclei 全部常用参数；单/多目标；实时日志；停止。
3. 历史记录持久化与详情（结果/日志/命令），结果严重度筛选，JSONL/TXT 导出。
4. 模板库浏览（-tl/-tgl）、点击填入。
5. 历史漏洞 → YAML 模板生成（内置模板优先，否则骨架）、查看/保存/修改/复用；我的模板管理。
6. 设置：引擎路径、默认参数、主题。
7. 联网：网络连通检测；更新失败自动回退镜像下载模板/nuclei。
8. Kali `sudo ./start.sh` 一键启动；免登录；数据不丢。

## 9. 第一版未完成的功能（已于 2026-08-31 全部完成）

> 以下各项在本轮开发中已实现并通过自动化测试（见第 10 节）：

1. **数据备份与恢复** ✅：设置页一键把 data/ + custom-templates/ 打包 zip 下载；zip 导入恢复（先备份当前数据再覆盖），含 zip-slip 防护与结构校验。
2. **模板校验** ✅：YAML 编辑器内调用 `nuclei -validate` 校验自定义模板语法，实时反馈通过/失败详情。
3. **报告导出增强** ✅：Markdown 报告导出（按严重程度分组统计）；历史详情「重新扫描」一键用相同目标与参数重扫。
4. **结果去重与严重度统计** ✅：详情页各严重程度数量徽章；一键去重（按 template-id+matched-at，保留最高严重度）。
5. **多请求头友好输入 + 排除严重程度** ✅：自定义请求头改为多行文本输入；扫描页新增「排除严重程度 (-es)」选择，映射 `-es`。

## 10. 可以实际检查验收的标准

### 10.1 自动化测试（可执行、可复算）
- 命令构造：给定参数集合，断言生成的 nuclei 参数正确（含 -l 文件、过滤、速率、输出、-duc 等）。
- JSONL 解析：构造样例文件，断言行数与字段。
- 端到端扫描：用 stub nuclei 模拟执行，断言记录状态 completed、结果计数正确、日志可读、结果可解析。
- HTTP 服务：断言各 API 状态码与返回结构。
- 自定义模板：生成/保存/读取/列表/删除、文件名防穿越。
- 备份恢复：备份 zip 生成、恢复后数据一致。
- 校验接口：stub 返回通过/失败两种结果，断言前端可感知状态。
- 报告导出：MD/JSONL/TXT 内容断言。

### 10.2 人工验收（浏览器）
- 打开应用即见扫描页，顶部显示引擎状态；深/浅主题可切换且主色为蓝。
- 启动扫描（有 nuclei 环境）→ 实时日志滚动 → 结束后历史出现记录，结果数与实际一致。
- 历史详情：结果/日志/命令三个标签正确；YAML 按钮生成模板并保存到我的模板；删除、使用正常。
- 设置：保存默认参数生效；网络检测返回各源可达性；模板更新在无官方源时走镜像。
- 数据持久化：重启后端后历史、结果、自定义模板仍在。
- 备份/恢复：导出 zip 文件可下载；导入后数据恢复一致。

---

## 附：需求确认记录
- 2026-08-31 确认需求：Win11 风格、适配 Kali、数据保存在 nuclei 同级且重启不丢。
- 2026-08-31 追加：Kali `sudo` 一键启动、免登录、联网（官方连不上自动回退镜像站下载模板/nuclei）。
- 2026-08-31 追加：主题色改为蓝色；历史漏洞可生成/查看/保存/修改/复用 YAML 模板。
